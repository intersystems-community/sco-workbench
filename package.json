{
  "name": "sco-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "SCO Workbench — MVP AI agent for IRIS cube & interoperability automation",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "start": "npm run start --workspace backend",
    "test": "npm run test --workspace backend; B=$?; npm run test --workspace frontend -- --watch=false; F=$?; exit $((B|F))",
    "test:unit": "npm run test:unit --workspace backend",
    "test:it": "npm run test:it --workspace backend",
    "test:e2e": "npm run test:e2e --workspace backend",
    "test:agent-eval": "npm run test:agent-eval --workspace backend",
    "lint:no-highcharts": "node ci/assert-no-highcharts.mjs",
    "cleanup:test": "npm run cleanup:test --workspace backend",
    "seed:example-cube": "npm run seed:example-cube --workspace backend"
  },
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  }
}
