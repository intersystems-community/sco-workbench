{
  "name": "@sco-workbench/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "predev": "node scripts/compile-jdbc-helper.mjs",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run --project unit",
    "test:unit": "vitest run --project unit",
    "test:unit:ci": "vitest run --project unit --reporter=default --reporter=json --outputFile=vitest.json && git -c core.quotepath=false ls-files -- ':(glob)test/unit/**/*.test.ts' > .expected-test-files && node ../ci/assert-test-files.mjs vitest.json .expected-test-files --root \"$PWD\" \"the test/unit glob in backend/vitest.config.ts\"",
    "test:it": "vitest run --project integration",
    "test:it:ci": "vitest run --project integration --reporter=default --reporter=json --outputFile=vitest-it.json && git -c core.quotepath=false ls-files -- ':(glob)test/integration/**/*.it.test.ts' > .expected-it-files && node ../ci/assert-test-files.mjs vitest-it.json .expected-it-files --root \"$PWD\" \"the test/integration glob in backend/vitest.config.ts\"",
    "test:e2e": "vitest run --project e2e",
    "test:e2e:ci": "vitest run --project e2e --reporter=default --reporter=json --outputFile=vitest-e2e.json && git -c core.quotepath=false ls-files -- ':(glob)test/e2e/**/*.e2e.test.ts' > .expected-e2e-files && node ../ci/assert-test-files.mjs vitest-e2e.json .expected-e2e-files --root \"$PWD\" \"the test/e2e glob in backend/vitest.config.ts\"",
    "test:live": "vitest run --project live-source",
    "test:live:ci": "vitest run --project live-source --reporter=default --reporter=json --outputFile=vitest-live.json && git -c core.quotepath=false ls-files -- ':(glob)test/live-source/**/*.live.test.ts' > .expected-live-files && node ../ci/assert-test-files.mjs vitest-live.json .expected-live-files --root \"$PWD\" \"the test/live-source glob in backend/vitest.config.ts\"",
    "test:agent-eval": "vitest run --project agent-eval",
    "cleanup:test": "tsx scripts/cleanup-test-artifacts.ts",
    "seed:example-cube": "tsx scripts/seed-example-cube.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "@aws-sdk/client-s3": "^3.700.0",
    "@intersystems/intersystems-iris-native": "^2.0.3",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "basic-ftp": "^6.2.0",
    "better-sqlite3": "^11.10.0",
    "dotenv": "^17.0.0",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.7",
    "multer": "^2.2.0",
    "ssh2": "^1.17.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^20.19.0",
    "@types/pg": "^8.23.1",
    "@types/ssh2": "^1.15.5",
    "pg": "^8.23.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
